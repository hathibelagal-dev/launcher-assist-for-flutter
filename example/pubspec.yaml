name: launcher_assist_example
description: Demonstrates how to use the launcher_assist plugin.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  launcher_assist:
    path: ../

flutter:
  uses-material-design: true

